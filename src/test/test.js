import AnimatedTyping from '../';

const animationSkying = new AnimatedTyping('skiyng', {speed: 100}).animate();
const animationWindsurfing = new AnimatedTyping('windsurfing', {speed: 200}).animate();